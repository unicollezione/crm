<%= turbo_frame_tag 'customers' do %>
  <%= select_tag :customer_id, options_from_collection_for_select(
    @customers, :id, :nickname, { prompt: 'Select a customer' }
  ),
  class: 'form-control',
  'data-controller': 'orders',
  'data-orders-target': 'customer'
%>
<% end %>
